[package]
name = "http_parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "http_parser"
path = "src/lib.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
http = "1.0"